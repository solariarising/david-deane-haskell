# NEXT QUICK SWAPS

These are the easiest later changes that can be made quickly without disturbing the technical foundation.

## Homepage

- Swap the hero image if a stronger DDH-approved asset becomes available.
- Replace the hero headline.
- Replace the hero subhead.
- Change the primary CTA label.
- Change the secondary CTA target from `/books` to `/vault` or `/about`.
- Reorder the `Past / Present / Future` cards.
- Relabel the `Past / Present / Future` cards if a better framing emerges.

## Books / Conversion

- Reorder the three top entry cards on the Books page.
- Change which book is treated as the first recommended starting point.
- Swap `Tommytune` / `Emergence` language to emphasize free entry more or less aggressively.
- Tighten or expand individual book blurbs.

## Visual Polish

- Reduce or increase the homepage hero overlay strength.
- Swap the CTA styling intensity without changing layout.
- Compress or replace `vault-books-composite.jpg` if lighter media is needed later.

## Agent / SEO Layer

- Update route descriptions in `src/seo.ts`.
- Update `public/ai-summary.html` when the canon language evolves.
- Adjust OG/social images in `public/social/`.

## Fastest Files To Edit

- `src/pages/Index.tsx`
- `src/pages/About.tsx`
- `src/pages/Books.tsx`
- `src/pages/Vault.tsx`
- `src/pages/Contact.tsx`
- `src/seo.ts`
- `public/ai-summary.html`
